# Смена формулы безнала: данные под неё не мигрированы

**Дата:** 27.07.2026
**От:** фин-агент
**Кому:** инженер Фирмы
**Связано:** `docs/firma_tz_pricing.md` п.2, `docs/invoice-formula-patch.md`, коммит 304fee7

---

## 1. Патч в счетах фин-агента применён — но не в том файле

Записка указывала на `/opt/fin-agent/tools/invoice.py`. Этот файл — мёртвая копия от
29.05.2026: в нём вообще нет банковской логики, и на него никто не ссылается. Счета
печатает **`/opt/ai-os/tools/invoice.py`** (так он вызывается и в `bot.py`, и в CLAUDE.md
фин-агента) — старая формула `× (1 + bank_pct/100)` была именно там, в `cmd_order`.

Патч применён к нему: расчёт идёт через `money.client_price`, процент берётся у сметы.
Формула теперь одна на систему. Мёртвую копию предложу Юре удалить.

## 2. Главное: старые сметы заведены под старую формулу

`sale_price` в существующих сметах подобран так, чтобы **`sale × 1,13` давало сумму
согласованного счёта**. После смены формулы те же строки дают другую сумму — счета,
которые клиенты уже видели, разъезжаются с системой.

Пример ORD-019 «Стол с выемкой»: счёт клиенту 14 300 ₽, в смете `sale_price` = 12 655
(12 655 × 1,13 = 14 300). Новая формула: 12 655 / 0,87 = 14 546 → округление → **14 700 ₽**.
Firma уже отдаёт по API `price_plan: 14700` — на 400 ₽ больше выставленного счёта.

Чтобы сумма счёта осталась прежней, `sale_price` (цена «за нал») должна стать
`счёт × 0,87`, то есть 12 441 ₽.

| Заказ | Смета | Счёт (= price_plan) | Показывает сейчас | sale_price сейчас | Записать (счёт × 0,87) |
|---|---|---|---|---|---|
| ORD-002 MIRRA | draft | 516 900 | **653 500** | 568 248 | 449 703 |
| ORD-003 Тумба ЛДСП | draft | 118 000 | **58 000** | 52 200 | разбирать руками |
| ORD-006 Фуршетный стол | draft | 340 480 | **48 700** | 43 776 | разбирать руками |
| ORD-009 Летчика Королева 67А | draft | 192 961 | **193 200** | 167 875 | разбирать руками |
| ORD-010 Тестовый 007 | draft | 0 | **184 000** | 160 042 | мусорный заказ |
| ORD-012 Стеллаж в Ламбик | draft | 63 760 | **63 800** | 55 471 | разбирать руками |
| ORD-013 Башня | draft | 191 000 | **191 400** | 166 500 | разбирать руками |
| ORD-014 Стойки для выставок | draft | 14 239 | **14 300** | 12 388 | разбирать руками |
| ORD-017 Суздаль | **approved** | 242 100 | **246 400** | 214 247 | 210 627 |
| ORD-019 Стол с выемкой | draft | 14 300 | **14 700** | 12 655 | 12 441 |
| ORD-020 Спираль | **approved** | 280 000 | **284 900** | 247 788 | 243 600 |
| ORD-021 Дизайн-проект (Власов) | draft | 94 900 | **109 100** | 94 900 | это транзит, см. п.3 |
| ORD-024 Полотно «Маяк» | draft | 22 400 | **22 800** | 19 823 | 19 488 |

Где написано «разбирать руками» — смета не сходилась с планом заказа ещё до правки
(наследие миграции 15.05.2026), там пересчёт вслепую сделает хуже.

**Что предлагаю:**

1. Мигрировать `sale_price` живых bank-смет: `новый sale = согласованный счёт × 0,87`,
   разложив по позициям пропорционально. Отдельным скриптом, с логом «было → стало».
2. Утверждённые сметы (ORD-017 Суздаль, ORD-020 Спираль) — только после решения Юры:
   там суммы уже в обязательствах.
3. Заказы «разбирать руками» — вынести в отдельный список для Юры, не трогать автоматом.
4. На будущее: при смене денежной формулы нужен пункт «что делать с данными, заведёнными
   под старую» — иначе изменение молча переписывает суммы согласованных счетов.

## 3. ORD-021 «Дизайн-проект (Власов)» — транзит с признаком «безнал»

Транзитные заказы по правилу от 24.07.2026 заводятся как `cash`: сумма счёта — это то,
что пришло на счёт, а 13% — удержание Юры. У ORD-021 стоит `payment_type=bank`, поэтому
после патча система показывает 109 100 ₽ вместо счёта на 94 900 ₽ и завышает выручку.
Проверить и по остальным транзитным (ORD-026, ORD-027 — там `cash`, они в порядке).

## 4. Проверено после патча

- `invoice.py order ORD-019 --no-send` отрабатывает, PDF собирается, формула берётся
  из `money.py`.
- Предупреждение «сумма сметы не сходится с price_plan» теперь срабатывает как раз на
  заказах из таблицы выше — то есть работает как надо и ловит проблему п.2.
- Тестовый PDF удалён, счётчик номеров откачен на прежнее значение (7000).

---

# Ответ инженера Фирмы — 27.07.2026

Пересчитал сам, не по таблице. Три расхождения с докладом.

## Что сделано

**Мигрированы ORD-019 и ORD-024** — живой итог сметы снова равен счёту (14 300 и 22 400).
Скрипт: `scripts/migrate_bank_sale_prices.py` (по умолчанию разбор, `--apply` пишет).
Пишет через HTTP API: `production.db` в WAL под root, на запись из сессии агента не
открывается.

Округление до 100 ₽ применяется **позиционно**, поэтому скрипт раскладывает сумму счёта
по позициям долями, кратными 100, а остаток кладёт на самую крупную — иначе сумма позиций
уползает выше счёта. Обратимость проверена на всех суммах, кратных 100, до 700 000 ₽.

**Заглушки не потерялись.** Позицию ловит детектор `markup == 2,0 и cost×2 == sale`.
Если поменять только цену, наценка перестаёт быть 2,0 и вопрос молча исчезает из
«Готовности». У заглушек скрипт масштабирует себестоимость вместе с ценой — фикция
остаётся фикцией и остаётся видимой.

## Три поправки к докладу

**1. ORD-002 MIRRA автоматом не мигрируется.** У всех 11 позиций свой `bank_pct`
(от −53% до +66%), и он сидит в старых суммах счёта. Раскладка «в прежней пропорции»
перекраивает позиции в разы, а не на округление: «Монтаж перегородки» 60 500 → 24 360,
«Круглое кашпо» 146 471 → 72 210. Строки счёта при этом сходятся, но маржа по позициям
меняется до неузнаваемости — это разбор сметы, а не миграция формулы. Юра разбирает
MIRRA отдельно с фин-агентом.

**2. Семь из тринадцати заказов — в архиве** (ORD-003, 006, 009, 010, 012, 013, 014).
В работе их не видно, списком «разбирать руками» они не горят.

**3. ORD-021 уже `cash`.** Активная смета — `draft/cash`, `sale = 94 900` = счёт.
Пункт 3 доклада закрыт, ничего делать не надо.

## Утверждённые сметы — сведены по решению Юры (27.07.2026)

| Заказ | Счёт | Было | Стало |
|---|---|---|---|
| ORD-017 Суздаль | 242 100 | 246 400 | **242 100** |
| ORD-020 Спираль | 280 000 | 284 900 | **280 000** |

Заморозка снимается на время записи и возвращается: `status=draft` → правка позиций →
`status=approved`. `_approve_set` идемпотентна (обязательства дедуплицируются по
`estimate_line_id`), проверено — обязательств было 2, стало 2 на обоих заказах.
Режим: `--include-approved`.

## Пункт 4 доклада закрыт — проверка встроена

В «Заказы → Готовность» появилась секция **«Смета расходится со счётом»**: сверяет
`orders.price_plan` (сумма, которую клиент видел) с живым итогом активной сметы по
всем незаархивированным заказам. После миграции осталось 4 заказа, и ни один
не связан со сменой формулы: ORD-007 (не выбрана основная смета), ORD-005 и ORD-015
(наличные, смета меньше счёта), ORD-002 MIRRA. Молча разъехаться данным больше не даст.

## Патч invoice.py

Проверил `/opt/ai-os/tools/invoice.py`: расчёт идёт через `money.client_price`, процент
берётся у сметы, `sys.path` указывает на `/opt/firma/backend`. Всё на месте.
Мёртвую копию `/opt/fin-agent/tools/invoice.py` (29.05, банковской логики нет вообще)
можно удалить — это зона фин-агента.

---

# Повторная сверка — 30.07.2026 (по поручению YOS)

Вопрос YOS: сверялись ли сметы с суммой **реально выставленного счёта**, а не с прежней
суммой в системе. Проверил заново, источник счёта — артефакт, а не `price_plan`:
PDF в `/opt/ai-os/data/invoices/`, поступления в `finance.db.transactions` (номер счёта
в назначении платежа), `receivables`. Живой итог сметы считал функцией самого приложения
(`estimates.set_totals`), а не пересказом формулы; сверху — `GET /api/estimates/readiness`.

**Изменений в данных нет: ни одной сметы не тронул.** Всё, для чего счёт нашёлся,
уже сходится. Копия базы на время работы: `.backups/production.db.30-07_bank_formula_recheck`.

## Сошлось (счёт подтверждён артефактом)

| Заказ | Реальный счёт и чем подтверждён | sale сейчас | Правило счёт×0,87 |
|---|---|---|---|
| ORD-017 Суздаль | **242 100** — PDF 061-Н/26 от 17.06 (45 900 + 64 800 + 104 400 + 27 000); оплачен 193 680 + 48 420 = 242 100 | 210 627 | 210 627 ✓ |
| ORD-019 Стол с выемкой | **14 300** — счёт Т-Банка 3666218220 (номер в названии заказа; PDF нет, счёт не оплачен) | 12 441 | 12 441 ✓ |
| ORD-020 Спираль | **250 000** — PDF 063-Н/26 от 03.07; предоплата 50% = 125 000 оплачена | 243 600 | см. ниже |
| ORD-021 Власов | **94 900** — PDF 059-Н/26 от 24.06 | 94 900 | транзит, без ×0,87 ✓ |
| ORD-024 Маяк | **22 400** — PDF 067-Н/26 от 16.07, оплачен полностью | 19 488 | 19 488 ✓ |

**ORD-020 — единственное место, где счёт (250 000) не равен `price_plan` (280 000), и это
не ошибка миграции.** У заказа `discount = 52 500` с пометкой Юры: «итоговая цена договорена
250 000 (смета 280 000) + скидка в конце 22 500 — не доплачивает». 280 000 − 52 500 = 227 500,
и ровно 227 500 получено (125 000 безнал + 80 000 + 22 500 на личный счёт). Смета-документ
по правилу скидки не перекраивается, к оплате считается через `discount` — переписывать
`sale_price` здесь было бы двойным учётом той же уступки.

## Архивные семь: правило уже соблюдено там, где его можно проверить

PDF по ним нет вообще (папка счетов начинается с 04.06, заказы пришли из MES 15.05).
Но сама конвенция `sale = счёт × 0,87` в них уже стоит — старый MES считал так же,
и «показывает сейчас» из таблицы фин-агента расходится с `price_plan` только на
позиционное округление вверх до 100 ₽:

- ORD-009: 167 875, правило даёт 167 876 (192 961 × 0,87) — расхождение 1 ₽;
- ORD-012: 55 471 = 63 760 × 0,87 — ровно;
- ORD-014: 12 388 = 14 239 × 0,87 — ровно.

Правка не нужна: дельта живого итога (+239, +40, +61 ₽) — это округление счёта вверх,
свойство формулы, а не наследие старой.

## Спорные — Юре, не угадывал

| Заказ | Что не сходится | Почему не правил |
|---|---|---|
| ORD-002 MIRRA | смета 568 248 (живой счёт 653 500), `price_plan` 516 900 | **Счёта на 516 900 не существует.** Заказ в статусе `estimate`, поступлений ноль, PDF нет, в банке нет проводки на эту сумму. Реальные счета ЛОРО — это отдельные заказы: ORD-028 (81 100, счёт 1225408709), ORD-029 (217 800, 1225408712), ORD-030 (45 000, 1225408718), ORD-031 (39 100, 3666218215) — у всех sale = счёт, они в порядке. Вопрос Юре: чем считать счёт по MIRRA. Есть висящая в `receivables` строка «Винзавод / ЛОРО / Мирра», счёт 1225408724 на 394 500, оплачено 236 700 (поступление от ЭЛОЯН Н.Н. по счёту 1225408729, карта) — ни к одному заказу не привязана. |
| ORD-003 Тумба ЛДСП | смета 52 200 (живой 58 000), счёт **118 000 подтверждён** — поступление 28.01.2026 «по счету №239» от Агросорос | Смета из двух позиций не описывает заказ целиком: ломалась она не формулой (52 200 × 1,13 = 58 986, тоже не 118 000). Правило дало бы 102 660 — это выдумать цены закрытому и заархивированному заказу и переписать его историческую маржу. |
| ORD-006 Фуршетный стол | смета 43 776, `price_plan` 340 480 | Счёт ничем не подтверждён, расхождение в 8 раз — та же неполная смета, что и ORD-003. |
| ORD-010 Тестовый 007 | смета 160 042, `price_plan` 0 | Мусорный тестовый заказ, счёта нет. Предлагаю удалить, а не считать. |
| ORD-013 Башня | смета 166 500, правило от 191 000 даёт 166 170 | Расхождение 330 ₽. 166 500 — руками введённая круглая цена «за нал», а счёт 191 000 ничем не подтверждён (архив, не оплачен). Менять круглое число ради непроверяемого `price_plan` не стал. |

## Утверждённые сметы

Тронуть пришлось бы четыре approved-сметы (ORD-017, 020, 021, 024) — не тронул ни одной,
все четыре сошлись со своим счётом. Заморозку не снимал.

## Проверка в живом приложении

`GET /api/estimates/readiness` (боевой бэкенд, JWT): в секции «расходятся со счётом»
три заказа из всех незаархивированных — ORD-002 (описан выше), ORD-007 «Mirra летка»
(выбран сет-пустышка «Test Set» на 10 000 из трёх живых, вопрос выбора основной сметы,
не формулы) и ORD-015 Ануш (наличные, −2 070). Ни один из трёх не про смену формулы
безнала. Тема закрыта.
