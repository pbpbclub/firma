Ночное код-ревью (review_lite) нашло 3 возможных дефектов в репо firma (/home/claude-runner/firma).

Находки:
- [med] backend/routers/orders.py:310 — ValueError на open_rest=None по-прежнему выбрасывается наружу из помощника, который списочный эндпоинт зовёт по каждой строке: один заказ in_production+оплачен с непосчитанным остатком роняет весь список 500 вместо пометки этой строки признаком «не посчитано».
- [low] deploy.sh:106 — `sleep 1` в конце итерации исполняется до проверки дедлайна, поэтому фактический потолок ожидания — 41 с при обещанных в комментарии, stderr и алерте 40 с, то есть тот же разрыв «объявленный срок ≠ факт», ради устранения которого и правился цикл.
- [low] backend/routers/finance.py:1812 — Аварийная ветка get_receivables вводит ключи `snoozed`, `duplicates_checked`, `snoozes_checked`, которых нет в диффе успешной ветки: если имена или наличие полей там расходятся, фронт получит флаги деградации только при полном отказе, а частичный сбой снова пройдёт как «данных нет».

Выполни строго по шагам:
1. КРИТИЧЕСКИЙ АНАЛИЗ. Открой код и проверь каждую находку: ревьюер видел только дифф, ложные срабатывания возможны. Не подтвердилась — НЕ чини, в отчёте напиши «не подтверждена: <причина>».
2. FIX подтверждённых. Сначала прочитай docs/code_rules.md репо и соблюдай правила. Только код: данные, базы, .env и конфиги сервисов не трогать, сервисы не перезапускать. Рискованный или неоднозначный фикс — пропусти и опиши предложение.
3. ПРОВЕРКА. python3 -m py_compile каждого правленого .py; у затронутых CLI запусти --help. Ничего боевого не гонять.
4. COMMIT (только если что-то починил): git add <только правленые файлы> && git commit -m "review_lite fixes: <что починил>" (пуш сделает ночной git_sync)
5. ОТЧЁТ. Краткий список: починено / не подтверждена (почему) / требует решения Юры.
   Последней строкой — счётчик ровно в таком виде: `ИТОГ: подтверждено N, не подтверждено M`.
   По нему считается точность ночного ревью, поэтому строка обязательна и без пояснений.