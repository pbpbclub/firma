Ночное код-ревью (review_lite) нашло 4 возможных дефектов в репо firma (/home/claude-runner/firma).

Находки:
- [high] backend/routers/orders.py:140 — Оба вызывающих подставляют `open_rest_by_order(...).get(id, 0.0)`, то есть «остаток не посчитан» превращается в честный ноль ещё до входа в помощник — ветка unknown недостижима из реального кода, и вся деградация живёт только в юнит-тесте.
- [high] backend/routers/orders.py:1237 — В карточке заказа отсутствие ключа в результате open_rest_by_order по-прежнему даёт 0.0, поэтому подсказка «похоже, завершён» выдаётся с done_open_rest=0 и done_open_rest_unknown=false даже когда остаток не считался.
- [med] backend/routers/orders.py:331 — Коммит обещает «во фронте — своя плашка», но фронтенд-файлов в диффе нет: при unknown подсказка просто исчезает с экрана, и человек видит ровно то же, что при честном отсутствии остатка.
- [low] backend/routers/orders.py:326 — log.warning стоит внутри помощника, который зовётся в цикле списочного эндпоинта, а order_id имеет дефолт None — один запрос списка даёт по записи на каждый заказ, а вызов без нового параметра пишет неатрибутируемое order_id=None.

Выполни строго по шагам:
1. КРИТИЧЕСКИЙ АНАЛИЗ. Открой код и проверь каждую находку: ревьюер видел только дифф, ложные срабатывания возможны. Не подтвердилась — НЕ чини, в отчёте напиши «не подтверждена: <причина>».
2. FIX подтверждённых. Сначала прочитай docs/code_rules.md репо и соблюдай правила. Только код: данные, базы, .env и конфиги сервисов не трогать, сервисы не перезапускать. Рискованный или неоднозначный фикс — пропусти и опиши предложение.
3. ПРОВЕРКА. python3 -m py_compile каждого правленого .py; у затронутых CLI запусти --help. Ничего боевого не гонять.
4. COMMIT (только если что-то починил): git add <только правленые файлы> && git commit -m "review_lite fixes: <что починил>" (пуш сделает ночной git_sync)
5. ОТЧЁТ. Краткий список: починено / не подтверждена (почему) / требует решения Юры.
   Последней строкой — счётчик ровно в таком виде: `ИТОГ: подтверждено N, не подтверждено M`.
   По нему считается точность ночного ревью, поэтому строка обязательна и без пояснений.